HTML -> Structure 
CSS -> Style 
Javascript -> Behavior

HTML
HTML stands for Hyper Text Markup Language
HTML is the standard markup language for creating Web pages
HTML describes the structure of a Web page
HTML consists of a series of elements
HTML elements tell the browser how to display the content

CSS
CSS stands for Cascading Style Sheets
CSS describes how HTML elements are to be displayed 
CSS saves a lot of work. It can control the layout of multiple web pages all at once
External stylesheets are stored in .css files


JavaScript
JavaScript is the world's most popular programming language.
JavaScript is the programming language of the Web.
JavaScript adds functionality to your web pages, validates forms, creates cookies, detects web browsers, and much more.
Scripts can be embedded within the head or body sections or both of an HTML page.
Alternatively, scripts can be stored as an external JavaScript file (file_name.js)


